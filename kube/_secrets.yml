apiVersion: v1
kind: Secret
metadata:
  name: aquarius-secrets
stringData:
  TOKEN: ${TOKEN}
  CLIENT_ID: "${CLIENT_ID}"
  SENTRY: ${SENTRY}
  DICTIONARY_API_KEY: ${DICTIONARY_API_KEY}
  TEST_BOT_TOKEN: ${TEST_BOT_TOKEN}
  HEARTHSTONE_KEY: ${HEARTHSTONE_KEY}
  TIMBER_KEY: ${TIMBER_KEY}
  SHOWTIMES_SERVER: ${SHOWTIMES_SERVER}
  SHOWTIMES_KEY: ${SHOWTIMES_KEY}
  GITHUB_API_TOKEN: ${GITHUB_API_TOKEN}
  DARK_SKY_API_KEY: ${DARK_SKY_API_KEY}
  MAPBOX_API_KEY: ${MAPBOX_API_KEY}
  FIREBASE_KEYFILE: ${FIREBASE_KEYFILE}
  FIREBASE_PROJECT: ${FIREBASE_PROJECT}
  ALPHA_VANTAGE_KEY: ${ALPHA_VANTAGE_KEY}
  GIT_HASH: ${GITHUB_SHA}
  FINANCIAL_MODELING_API_KEY: ${FINANCIAL_MODELING_API_KEY}
