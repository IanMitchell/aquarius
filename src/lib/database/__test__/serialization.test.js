import { serializeMap, deserializeMap } from '../serialization.js';

// Support for serializing maps is coming soon!
// Once it ships we can remove this code.
test.todo('Ensure native syntax not available yet');

describe('serializeMap', () => {
  test.todo('Converts Map into JSON Object');
});

describe('deserializeMap', () => {
  test.todo('Converts JSON Object into Map');
});
