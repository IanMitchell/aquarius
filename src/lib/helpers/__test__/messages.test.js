import { isDirectMessage, isBot, getLink } from '../messages.js';

describe('isDirectMessage', () => {
  test.todo('Only matches DM Channels');
});

describe('isBot', () => {
  test.todo('Does not trigger in Test Mode');

  test.todo('Identifies bots');
});

describe('getLink', () => {
  test.todo('Generates a link to a message');
});
